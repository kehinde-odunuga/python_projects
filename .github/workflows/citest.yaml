# name: Python CI

# on:
#   push:
#     branches: [dev, main]
#   pull_request:
#     branches: [dev, main]

# jobs:
#   test:

#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v3

#     - name: Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.x'  # You can specify the Python version here

#     - name: Install dependencies
#       run: |
#         python -m pip install --upgrade pip
#         if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
#         if [ -f setup.py ]; then pip install .; fi  # Optional if you're using setup.py

#     - name: Run all Python files
#       run: |
#         for file in $(find . -name "*.py" -not -path "./venv/*"); do
#           echo "Running $file"
#           python $file || exit 1
#         done